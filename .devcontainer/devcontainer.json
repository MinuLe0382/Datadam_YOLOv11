{
    "name": "YOLO v11 (CUDA)",
    "image": "nvidia/cuda:12.1.1-cudnn8-devel-ubuntu22.04",
    "runArgs": [
        "--gpus=all",
        "--shm-size=8g",
        "--ipc=host"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-toolsai.jupyter",
                "natqe.reload",
                "mechatroner.rainbow-csv"
            ],
            "settings": {
                "python.defaultInterpreterPath": "/usr/bin/python3"
            }
        }
    },
    "postCreateCommand": "apt-get update && apt-get install -y python3 python3-pip python3-dev python3-tk git build-essential pkg-config libgl1 libglib2.0-0 && pip install --upgrade pip setuptools wheel && pip install --no-cache-dir torch==2.1.1+cu121 torchvision==0.16.1+cu121 torchaudio==2.1.1+cu121 --index-url https://download.pytorch.org/whl/cu121 && pip install --no-cache-dir numpy==1.26.4 opencv-python-headless==4.8.1.78 monai==1.3.0 ultralytics pyyaml safetensors pillow tqdm matplotlib einops ipykernel && pip install --no-cache-dir -U pycocotools onnx onnxruntime || true"
}
